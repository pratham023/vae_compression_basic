#Vanilla RNN
import torch
import torch.nn as nn
import torch.optim as optim
import torch.nn.functional as F
from torch.utils.data import DataLoader
from torchvision.datasets import MNIST
from torchvision import transforms

import time

transform = transforms.Compose([
    transforms.ToTensor(),
    transforms.Normalize((0.5,), (0.5,))
])

train_data = MNIST('./data', train=True, transform=transform, download=True)
test_data = MNIST('./data', train=False, transform=transform, download=True)

train_loader = DataLoader(train_data, batch_size=128, shuffle=True)
test_loader = DataLoader(test_data, batch_size=128, shuffle=False)

class RNN(nn.Module):
    def __init__(self):
        super().__init__()

        self.hidden_size = 128
        self.num_layers = 1

        self.rnn = nn.RNN(
            input_size=28,
            hidden_size=128,
            num_layers=1,
            batch_first=True,
            nonlinearity='relu'
        )

        self.fc = nn.Linear(128, 10)

    def forward(self, x):
        x = x.squeeze(1)
        out, h_n = self.rnn(x)
        x = h_n[-1]
        x = self.fc(x)
        return x

rnn_model = RNN()
criterion = nn.CrossEntropyLoss()
optimizer = torch.optim.Adam(rnn_model.parameters(), lr=0.001)

for epoch in range(25):
    rnn_model.train()
    total_loss = 0

    for data, target in train_loader:
        optimizer.zero_grad()
        output = rnn_model(data)
        loss = criterion(output, target)
        loss.backward()
        optimizer.step()
        total_loss += loss.item()

    print(f"Epoch {epoch+1}, Loss: {total_loss/len(train_loader):.4f}")

rnn_model.eval()
correct = 0
total = 0

with torch.no_grad():
    for data, target in test_loader:
        output = rnn_model(data)
        _, predicted = torch.max(output, 1)
        total += target.size(0)
        correct += (predicted == target).sum().item()

print(f"Accuracy: {100 * correct / total:.2f}%")

all_params = torch.cat([p.detach().view(-1) for p in rnn_model.parameters()])
print(all_params.shape)

#VAE
def flatten_weights(model):
    return torch.cat([p.detach().view(-1) for p in model.parameters()])

def unflatten_weights(model, flat_vector):
    with torch.no_grad():
        pointer = 0
        for p in model.parameters():
            numel = p.numel()
            p.copy_(flat_vector[pointer:pointer + numel].view_as(p))
            pointer += numel

rnn_w = flatten_weights(rnn_model)

N_SAMPLES = 100
NOISE_STD = 0.01

weight_dataset = []
for _ in range(N_SAMPLES):
    noise = torch.randn_like(rnn_w) * NOISE_STD
    weight_dataset.append(rnn_w + noise)

weight_dataset = torch.stack(weight_dataset)

input_dim = weight_dataset.shape[1]
latent_dim = 64

class VAE(nn.Module):
    def __init__(self, input_dim, latent_dim):
        super().__init__()

        self.encoder = nn.Sequential(
            nn.Linear(input_dim, 1024),
            nn.ReLU(),
            nn.Linear(1024, 512),
            nn.ReLU()
        )

        self.mu = nn.Linear(512, latent_dim)
        self.logvar = nn.Linear(512, latent_dim)

        self.decoder = nn.Sequential(
            nn.Linear(latent_dim, 512),
            nn.ReLU(),
            nn.Linear(512, 1024),
            nn.ReLU(),
            nn.Linear(1024, input_dim)
        )

    def reparametrize(self, mu, logvar):
        std = torch.exp(0.5 * logvar)
        eps = torch.randn_like(std)
        return mu + eps * std

    def forward(self, x):
        h = self.encoder(x)
        mu = self.mu(h)
        logvar = self.logvar(h)
        z = self.reparametrize(mu, logvar)
        recon = self.decoder(z)
        return recon, mu, logvar

def vae_loss(recon, x, mu, logvar):
    recon_loss = F.mse_loss(recon, x, reduction='sum')
    kl = -0.5 * torch.sum(1 + logvar - mu.pow(2) - logvar.exp())
    return recon_loss + kl

vae_rnn = VAE(input_dim, latent_dim)
optimizer = torch.optim.Adam(vae_rnn.parameters(), lr=1e-3)

start_time=time.time()

for epoch in range(500):
    optimizer.zero_grad()
    recon, mu, logvar = vae_rnn(weight_dataset)
    loss = vae_loss(recon, weight_dataset, mu, logvar)
    loss.backward()
    optimizer.step()

    if epoch % 50 == 0:
        print(f"Epoch {epoch}, Loss: {loss.item():.2f}")

with torch.no_grad():
    h = vae_rnn.encoder(rnn_w.unsqueeze(0))
    mu = vae_rnn.mu(h)
    z = mu
    reconstructed_weights = vae_rnn.decoder(z).squeeze(0)

unflatten_weights(rnn_model, reconstructed_weights)

end_time=time.time()
training_time = end_time - start_time

rnn_model.eval()
correct = 0
total = 0

with torch.no_grad():
    for data, target in test_loader:
        output = rnn_model(data)
        _, predicted = torch.max(output, 1)
        total += target.size(0)
        correct += (predicted == target).sum().item()

print(f"RNN Accuracy after VAE reconstruction: {100 * correct / total:.2f}%")
print(f"Training time: {training_time:.2f} seconds")
